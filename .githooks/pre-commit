#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"

echo "pre-commit: running Rust format check (mcp-server)"
cd "$repo_root/mcp-server"

cargo fmt --check

echo "pre-commit: format check passed"
